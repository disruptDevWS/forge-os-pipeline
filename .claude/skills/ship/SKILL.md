# Ship
1. Run `npx tsc --noEmit` and fix any errors
2. Run `git add -A && git status` and show the summary
3. Write a conventional commit message based on the changes
4. Commit and push to the current branch
5. Update DECISIONS.md and MEMORY.md if significant changes were made
6. Confirm the commit hash and branch name
